# claude-fleet has no build artifacts; runtime state lives in each workspace's
# .fleet/ dir, never in this repo. Ignore the usual local noise.
*.tmp
*.tmp.*
*~
.DS_Store

# Internal/per-workspace state — must never be committed (this repo is public).
# .fleet/ = local fleet manifest + worker config; .claude/ = local Claude settings.
.fleet/
.claude/
